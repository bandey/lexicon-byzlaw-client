import styled from 'styled-components';
import Col from 'react-bootstrap/Col';

const ColTitle = styled(Col)`
  padding-bottom: 1rem;
`;

const Title = styled.h1`
  margin-top: 0.25rem;
  margin-bottom: 0;
  font-size: 1.5rem;
`;

const ColLang = styled(Col)`
  padding-bottom: 1rem;
`;

export {ColTitle, Title, ColLang};
