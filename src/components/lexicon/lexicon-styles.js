import styled from 'styled-components';

import {PanelHeader} from '../panel/panel.js';

const PanelHeaderForList = styled(PanelHeader)`
  padding-left: 0;
  padding-right: 10px;
`;

export {PanelHeaderForList};
